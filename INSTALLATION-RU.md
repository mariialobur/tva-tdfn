# Установка версии 10.1 reviewed

## Что загрузить в корень репозитория

Замените существующие файлы:

- `index.html`
- `styles.css`
- `data.js`
- `logic.js`
- `store.js`
- `components.js`
- `transition.js`
- `app.js`
- `.nojekyll`

Не загружайте ZIP как файл сайта: сначала распакуйте архив.

## После загрузки

1. Дождитесь зелёного статуса GitHub Actions / Pages.
2. Откройте сайт в приватном окне или выполните Ctrl+Shift+R.
3. Проверьте URL:
   - `#cas-A`
   - `#cas-D3`
   - `#cas-D4`
   - `#cas-K2`
   - `#cas-L5`
   - `#cas-L7`
   - `#cas-R`
4. Откройте блок лимитов и убедитесь, что видны обе разные цифры: CHF 5’024’000 для TDFN и CHF 5’005’000 для décompte annuel.
5. Проверьте экспорт прогресса, затем импорт созданного JSON.
6. На первом кейсе кнопка «Предыдущий» должна быть отключена; на последнем — «Следующий».

## Очистка старых файлов

После успешной проверки можно удалить старые файлы:

- `v7-enhancements.*`
- `v8*-learning-path.*`
- `ui-fixes.*`

Они не подключаются новой версией.
